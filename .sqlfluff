[sqlfluff]
dialect = snowflake  # Ensure this matches your database
templater = dbt
max_line_length = 120  # Optional: Adjust as needed

[sqlfluff:templater:dbt]
project_dir = ./Dbt-project/snowflake_proj
