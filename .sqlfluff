[sqlfluff]
dialect = snowflake  # Use your database dialect, e.g., snowflake, postgres, bigquery, etc.
templater = dbt
max_line_length = 120  # Adjust as needed

[sqlfluff:templater:dbt]
project_dir = ./Dbt-project/snowflake_proj

[sqlfluff:rules]
exclude_rules = L009  # Optional: Exclude specific rules
